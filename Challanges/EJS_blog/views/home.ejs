<%- include('partials/header'); -%>

<h1>Home</h1>
<% if(posts.length < 1){ %>
  <p><%= homeStartingContent; %></p>
<% } else { %>

  <!-- for Loop -->
  <!--<% for(let i=posts.length-1; i>=0; i--){ %>
     <h3><%= posts[i].title; %></h3>
    <p><%= posts[i].body; %></p>
  <% }; %> -->

  <!-- forEach -->
  <% posts.forEach((post) => { %>
    <h3><%= post.title; %></h3>
    <p><%= post.body; %></p>
  <% }); %>

<% }; %>

<%- include('partials/footer'); -%>
